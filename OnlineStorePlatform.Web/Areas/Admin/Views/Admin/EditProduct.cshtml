@model OnlineStorePlatform.Models.ViewModels.Products.EditProductVm
@{
    ViewBag.Title = "Edit Product";
}
<link href="~/Content/customInput/component.css" rel="stylesheet" />
<link href="~/Content/customInput/normalize.css" rel="stylesheet" />
<link href="~/Content/Site.css" rel="stylesheet" />
<h1>Edit Product</h1>
@using (Html.BeginForm("EditProduct", "Admin", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="row edit-product">
        <div class="imgwrapper">
            <img class="img-responsive" src="~/IMG/Products/@Html.DisplayFor(vm=>vm.ImageUrl)" alt="Category" />
        </div>
        <div>
            <input type="file" name="ImageFile" id="file-2" class="inputfile inputfile-2" data-multiple-caption="{count} files selected" multiple="">
            <label for="file-2"><svg xmlns="http://www.w3.org/2000/svg" width="20" height="17" viewBox="0 0 20 17"><path d="M10 0l-5.2 4.9h3.3v5.1h3.8v-5.1h3.3l-5.2-4.9zm9.3 11.5l-3.2-2.1h-2l3.4 2.6h-3.5c-.1 0-.2.1-.2.1l-.8 2.3h-6l-.8-2.2c-.1-.1-.1-.2-.2-.2h-3.6l3.4-2.6h-2l-3.2 2.1c-.4.3-.7 1-.6 1.5l.6 3.1c.1.5.7.9 1.2.9h16.3c.6 0 1.1-.4 1.3-.9l.6-3.1c.1-.5-.2-1.2-.7-1.5z"></path></svg> <span>Choose a file…</span></label>
        </div>

        <div class="form-group">
            @Html.LabelFor(vm => vm.Name):
            @Html.TextBoxFor(vm => vm.Name, new { @class = "form-control border-color-6" })
        </div>
        <div class="form-group">
            @Html.LabelFor(vm => vm.CategoryName):
            @Html.TextBoxFor(vm => vm.CategoryName, new { @class = "form-control border-color-2", disabled = "disabled" })
        </div>

        <div class="form-group">
            @Html.LabelFor(vm => vm.Price):
            @Html.TextBoxFor(vm => vm.Price, new { @class = "form-control border-color-5" })
        </div>
        <div class="form-group">
            @Html.LabelFor(vm => vm.Description):
            @Html.TextAreaFor(vm => vm.Description, new { @class = "form-control border-color-3", rows = "7", cols = "30" })
        </div>

        <div class="form-group">
            <input type="submit" value="Save Changes" class="form-group btn btn-warning btn-lg" />
        </div>
    </div>
}
@section Scripts{
    <script src="~/Scripts/jquery.custom-file-input.js"></script>
}